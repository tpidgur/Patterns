
Опис:
основной шаблон проектирования, в котором объект внешне выражает некоторое поведение, но в реальности передаёт
ответственность за выполнение этого поведения связанному объекту.
Позволяет изменить поведение конкретного экземпляра объекта вместо создания нового класса путём наследования.
_______________
Реалізація:
1. Є інтерфейс Graphics з методом draw()
2. Є класи Triangle,Square,Circle, які імплементують даний інтерфейс
3. Є клас  Painter, який має поле Graphics та два методи: встановлення конкретного обєкту Graphics та виклик
його метода draw()
4. В методі main створюється обєкт Painter, якому на вхід по черзі встановлюється відповідна графіка
______________
Використання:
1)Шаблон делегирования является фундаментальной абстракцией, на основе которой реализованы другие шаблоны -
 композиция (также называемая агрегацией), примеси (mixins) и аспекты (aspects).
