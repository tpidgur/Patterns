

Опис:
Строитель (англ. Builder) — порождающий шаблон проектирования предоставляет способ создания составного объекта.
Отделяет конструирование сложного объекта от его представления, так что в результате одного и того же процесса
конструирования могут получаться разные представления.

____________
Реалізація:
1. клас Car з полями: make,transmission,maxSpeed; setters для кожного поля
2. клас CarBuilder з полями по замовчуванню (аналогічними Car); 3 методи, кожен з яких на вхід отримує одне
із полів, а не виході повертає сам себе; метод build, який повертає   Car та встановлює по замовчуванню 3 поля.
3. в методі main до обєкта CarBuilder додається різний функціонал, а потім метод build



_____________
Використання:
1)алгоритм создания сложного объекта не должен зависеть от того, из каких частей состоит объект и как они стыкуются
 между собой;
2)процесс конструирования должен обеспечивать различные представления конструируемого объекта.