
Опис:
Iterator — поведенческий шаблон проектирования.
забезпечує ітерацію невідомих даних за допомогою окремо прописаного ітератора всередині кожного класа.

___________
Реалізація:
1) Є інтерфейс Iterator  з 2ма методами hasNext та next.
2) Є інтерфейс Container, який повертає обєкт типу Iterator (метод "Iterator getIterator()")
3) Є довільний класс Tasks, який імплементує інтерфейс Container та реалізує його метод getIterator()
у вигляді повернення екземпляра класа TaskIterator.
TaskIterator це  inner private клас, який виключно для класа Tasks перевизначає методи hasNext та next
4)В методі main створюється обєкт класа Tasks та повертається обєкт типу Iterator через метод getIterator()
та відбувається ітерація по всім елементам.
_____________
Використання:
1)Особенностью полноценно реализованного итератора является то, что код,
использующий итератор, может ничего не знать о типе итерируемого агрегата
2)появляется возможность работы с диапазонами итераторов, при отсутствии знания о типе итерируемого агрегата.