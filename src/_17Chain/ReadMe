
Опис:
"цепочка ответственности" -поведенческий шаблон проектирования.
сообщения в системе по цепочке передаются всем обработчикам, который сам обрабатывает или нет
и отправляет другим на обработку.

_________________
Реалізація:
1. Клас Level з трьома  константами, які характеризують подію: ERROR = 1; DEBUG = 2; INFO = 3;
2. інтерфейс Logger з абстрактним методом "void writeMessage(String message, int level)"
3. 3 класи, які реалізують інтерфейс Logger: SMSLogger, FileLogger, EmailLogger,
 кожен з них має 2 поля: пріорітет, за якого цей клас спрацьовує та абстрактне посилання на наступний Logger
 4.в методі main cтворюються три класи-реалізатори Logger з вказанням пріоритету,коли спрацьовують
 вказується порядок класів , в якому обробка передається від одного до іншого
 а також декілька прикладів, коли на вхід одного логера приходить повідомлення зі своїм рівнем пріоритета,
  і воно опрацьовується послідовно на всіх логерах.


_________________
Використання:
1) в разрабатываемой системе имеется группа объектов, которые могут обрабатывать сообщения определенного типа;
2) все сообщения должны быть обработаны хотя бы одним объектом системы;
3) сообщения в системе обрабатываются по схеме «обработай сам либо перешли другому», то есть одни сообщения
 обрабатываются на том уровне, где они получены, а другие пересылаются объектам иного уровня.